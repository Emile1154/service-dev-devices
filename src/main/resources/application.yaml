spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/spring_db
    username: admin
    password: 250802wasd
    tomcat:
      validation-query: SELECT 1
      test-while-idle: true

  mail:
    host: smtp.yandex.ru
    port: 465
    username: laim778@yandex.ru
    password: bvemnrerwyxdleix
    protocol: smtps
  sql:
    init:
      mode: always